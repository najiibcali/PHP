# PHP Practice - Day One

## Overview

This section covers the basic syntax and output functionality of PHP.

## Topics Covered

- PHP opening and closing tags
- `echo`
- `print`
- Variables
- String interpolation
- `strlen()`
- `str_word_count()`

## 1. Displaying Output with echo

I used the echo statement to display output in PHP.

**Output:**

Welcome to PHP

I also practiced displaying multiple values using echo.

## 2. Displaying Output with print

I used the print statement to display output in PHP.

**Output:**

Welcome to PHP

## 3. Variables

I declared and initialized a variable named $Fullname to store my full name.

I then displayed the value of the variable using both echo and print.

I also practiced using the variable inside both single-quoted and double-quoted strings.

## 4. String Functions

### `strlen()`

I used the `strlen()` function to determine the length of a string.

It returns the total number of characters contained in the string.

### `str_word_count()`

I used the `str_word_count()` function to determine how many words are contained in a string.

It returns the total number of words in the given string.